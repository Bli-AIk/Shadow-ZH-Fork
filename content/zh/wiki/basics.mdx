export const metadata = {
    title: '理解基础概念',
    description: 'Kristal 代码的基础知识。'
}

# 了解基础知识

<Box>
Kristal 是一款用 Lua 编写、基于 LÖVE 的 DELTARUNE 引擎。它的设计尽可能接近原始游戏，同时仍然易于使用和修改。本页将介绍 Kristal 代码的基础知识以及使用方法。
</Box>

<Box>
## 学习 Lua
---

该引擎希望你对编程有基本的了解。

学习 Lua 的资料有很多，但最值得先看的是 [Lua 手册](https://www.lua.org/manual/5.1/)。内容比较密集，但它是最完整的参考资料。

不过，如果你想看更容易理解的内容，[这个教程](/wiki/lua-tutorial) 是一个不错的起点。

**在继续之前了解 Lua 非常重要。**
</Box>

<Box>
## 类、实例和继承
---

如果你熟悉面向对象编程，就会知道什么是类；这里也是同样的概念。

虽然类不是 Lua 的功能，但该语言使它们非常容易实现。但什么是“类”？

---

### 什么是类？

类是一个模板。它包含一组变量和函数。 **实例**是类的副本。它具有类的所有变量和函数，但可以更改它们而不影响类本身。


#### 汽车的例子

以汽车为例。假设“汽车”是一个类。
它有四个轮子、车窗、方向盘等。这就是汽车。
但世界上存在多种汽车（几乎 15 亿辆！），因此我们将它们称为实例。
它们都有共同点，但颜色可能不同，门的数量也不同，等等。

汽车的概念是一个类，每辆车都是该类的一个实例。在 Kristal 的编程中，我们会执行以下操作：

```lua
local Car, super = Class()

function Car:init()
    super.init(self)

    self.color = "red"
end

function Car:getWheelCount()
    return 4
end

function Car:setColor(color)
    self.color = color
end

return Car
```

创建该类的新实例就像调用 `Car()` 一样简单。当你创建新实例时，会调用 `init` 函数；这里就是设置实例的地方。

`self` 是一个特殊变量，指的是实例本身。因此，设置 `self.color` 就是在设置*该实例*的颜色。

`super` 也是一个特殊变量，指的是**父类**。稍后我们会讨论父对象和继承。

```lua
local new_car = Car() -- creates a new instance of the car class, or more simply, creates a new car.

print(new_car:getWheelCount()) -- prints "4"
print(new_car.color) -- prints "red"

new_car:setColor("blue") -- sets the color of the car to blue
print(new_car.color) -- prints "blue"
```

我们只是将汽车的颜色更改为蓝色，但类本身仍然将颜色预定义为红色。 **记住，类就像模板。**

---

### 继承

类可以有一个父类。这就是所谓的**继承**。子类*继承*父类的所有变量和函数，但也可以拥有自己的变量和函数。

假设我们想制造一辆赛车。它仍然是一辆汽车，但有扰流板和不同的颜色。我们可以创建一个名为 `Racecar` 的新类，它继承自 `Car`。

```lua
local Racecar, super = Class(Car)

return Racecar
```

我们刚刚创建了另一个类，它是 `Car` 类的扩展。尽管上面的代码中没有任何函数，但它仍然具有 `Car` 类的所有函数：

```lua
local new_racecar = Racecar()
print(new_racecar:getWheelCount()) -- prints "4"
```

现在，还记得之前的 `super` 变量吗？如果我们重写父类的函数，我们可以用它来调用它们。

```lua
local Racecar, super = Class(Car)

function Racecar:init()
    super.init(self)

    self.color = "blue"
end

return Racecar
```

上面的示例调用父类 `Car` 的 `init` 函数，然后将赛车实例的颜色设置为蓝色。

**super 函数应该用点号调用，而不是冒号！它们还应该将 `self` 作为第一个参数！**

</Box>

<Box>
## 对象
---

对象是与 Kristal 交互的主要方式。它们是会被绘制到屏幕上的类，拥有位置、层次结构和许多其他属性。

---
### 层次结构

这个词很难拼写，不是吗？

对象可以有子对象。子对象的位置是相对于父对象的。如果你移动父对象，子对象也会随之移动。

父对象会绘制它的子对象，子对象也会绘制自己的子对象，依此类推。父对象也会更新子对象。我们稍后会讨论绘图和更新。

把它想象成一棵树。父对象是树干，子对象是树枝。如果移动树干，树枝也会随之移动。

这些子对象——树枝——还可以拥有自己的子对象——叶子。如果移动树干，树枝会随之移动，叶子也会跟着树枝移动。

这意味着**每个对象都有一个父对象**。唯一的例外是舞台。

---
### 舞台

舞台是层次结构的根。它位于这棵树的顶端，就像连接树干的树根。

舞台是一个特殊的对象，是唯一没有父对象的对象。它是一切的开始：第一个被绘制和更新的对象，并负责绘制和更新自己的子对象，依此类推。

---
### 更新和绘图

对象有一些非常重要的功能。最重要的是 `update` 和 `draw`。

`update` 会在每帧调用，因此你可以在其中放置需要每帧运行的代码。比如在屏幕上移动某些内容、检查按钮是否按下，或执行其他每帧都要完成的操作。

`draw` 也会在每帧调用，这里才是实际将图像、形状和文本绘制到屏幕上的地方！
</Box>
